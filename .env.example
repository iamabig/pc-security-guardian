# Neon Database Connection
DATABASE_URL=postgresql://user:password@endpoint/neondb

# Security Configuration
# USER_API_KEYS are provided via the UI and not stored on the server.
# Only system-level context retrieval uses the DATABASE_URL.
